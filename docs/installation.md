# Installation

Copy `custom_components/carpiquet_ems` to `/config/custom_components/carpiquet_ems`, restart Home Assistant, then add Carpiquet EMS.

When upgrading from v0.1.0-alpha, the existing integration entry may need to be removed and recreated to load the new entity IDs and configuration fields cleanly.
