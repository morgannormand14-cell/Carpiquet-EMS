# Carpiquet EMS v0.4.3-alpha — Sprint 5 hotfix

- Fixes first-step Config Flow submission error (`extra keys not allowed`).
- Uses a stable explicit schema for Home Assistant selectors.
- Keeps AB2000X / AB3000L / I2400 dropdown.
- Keeps exact five-digit battery serial validation.
- Keeps dashboard corrections and simplified labels from v0.4.2.
- Keeps LIVE / FALLBACK ACTIF behavior.
- Simulation-only safety is unchanged.
